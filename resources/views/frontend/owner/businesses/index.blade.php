@extends('frontend.layouts.app')
@section('title','Businesses for Sale') @section('content')
<div style="background:#172033;padding:42px 0;color:#fff"><div class="container"><h1>Businesses for Sale</h1><p>Acquire running businesses, brands, franchises and operating assets.</p></div></div>
<div class="container" style="padding:28px 0 55px"><form method="GET" class="row g-2 mb-4"><div class="col"><input class="form-control" name="city" placeholder="City"></div><div class="col"><select class="form-control" name="category"><option value="">All Industries</option>@foreach($categories as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></div><div class="col"><input class="form-control" name="min_price" placeholder="Min ₹"></div><div class="col"><input class="form-control" name="max_price" placeholder="Max ₹"></div><div class="col-auto"><button class="btn btn-danger">Search</button></div></form>
<div class="row g-3">@forelse($businesses as $b)<div class="col-md-4"><a href="{{ url('/business/'.$b->slug) }}" style="text-decoration:none;color:inherit"><div style="border:1px solid #e5e7eb;border-radius:15px;overflow:hidden"><div style="height:180px;background:#f1f5f9">@if($b->cover_image)<img src="{{ asset('images/businesses/'.$b->id.'/'.$b->cover_image) }}" style="width:100%;height:100%;object-fit:cover">@endif</div><div style="padding:15px"><small>{{ $b->category_name }}</small><h3>{{ $b->is_confidential ? 'Confidential '.$b->category_name.' Business' : $b->title }}</h3><div>{{ $b->city }}, {{ $b->state }}</div><strong>₹{{ number_format($b->asking_price) }}</strong></div></div></a></div>@empty<div>No approved businesses found.</div>@endforelse</div>{{ $businesses->links() }}</div>@endsection